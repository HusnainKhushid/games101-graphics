# Notes

One file per topic block — rasterisation, geometry, ray tracing, animation.

Graphics notes are worth keeping in a specific way: the maths is standard and in the
slides, but *why a particular derivation is set up the way it is* is not. Write that down.
